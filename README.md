# next-weather

## Requirements

- an OpenWeatherMap key with access to the Free tier
- a MapTiler key with access to the Free tier

Place these keys in `.env.local` in the repository root, otherwise the app won't work